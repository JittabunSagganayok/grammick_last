import 'package:flutter/material.dart';

class paymentPromoButton extends StatelessWidget {
  const paymentPromoButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}